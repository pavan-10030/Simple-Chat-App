<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChatsChat</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>All Chats</h3>
    <form method="GET" action="/chats/new">
        <button>New Chat</button>
    </form>
    <%for(let chat of allChats){%>
    <div class="chat">
        <p>From : <%=chat.from%></p>
        <div class="message">
            <p><%=chat.msg%></p>
            <form method="GET" action="/chats/<%=chat._id%>/edit"><button>Edit</button></form>
            <form method="POST" action="/chats/<%=chat.id%>?_method=DELETE"><button>Delete</button></form>
        </div>

        <p>Received by : <%=chat.to%></p>
        <p><%=chat.createdAt.toString().split(" ")[4]%> <%=chat.createdAt.toString().split(" ").slice(0,4).join("-")%></p>
    </div>
    
    <%}%>

</body>
</html>
